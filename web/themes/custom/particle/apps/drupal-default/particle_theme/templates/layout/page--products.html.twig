{{ attach_library('particle/page.products') }}
{{ attach_library('particle/client_filter') }}

{% if not has_components_module %}
  {% include '@particle/misc/components--error-message.html.twig' %}
{% else %}
  {% include '@templates/generic/_generic.twig' with {
    "full_name": full_name,
    "user_picture": user_picture,
    "user_id": user_id,
    "clients": clients,
    "primary_menu": page.primary_menu,
    "highlighted": page.highlighted,
    "header_branding": page.header_branding,
    "secondary_menu": page.secondary_menu,
    "help": page.help,
    "breadcrumb": page.breadcrumb,
    "sidebar_first": page.sidebar_first,
    "content": page.content,
    "sidebar_second": page.sidebar_second,
    "footer": page.footer
  } %}
{% endif %}
