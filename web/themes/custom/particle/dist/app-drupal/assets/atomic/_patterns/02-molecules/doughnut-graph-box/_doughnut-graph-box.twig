{#
/**
 * @file
 * doughnut-graph-box
 *
 * Available variables for doughnut-graph-box:
 *  - doughnut_graph_box.variable_name [variable_type]: Description of the variable. Example below.
 *  - doughnut_graph_box.invert [bool]: When set, colors will be inverted for light display on dark background.
 */
#}

<div class="card m-5 {{ additional_class }}">
  <header class="card-header">{{ title }}</header>
  <div class="card-body">
    <canvas id="{{ id }}" class="mx-auto" height="200" width="200"></canvas>
  </div>
</div>
