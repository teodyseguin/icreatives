{#
/**
 * @file
 * doughnut-graph-box
 *
 * Available variables for doughnut-graph-box:
 *  - doughnut_graph_box.variable_name [variable_type]: Description of the variable. Example below.
 *  - doughnut_graph_box.invert [bool]: When set, colors will be inverted for light display on dark background.
 */
#}

<div class="card my-5 {{ additional_class }}">
  <header class="card-header">{{ title }}</header>
  <div class="card-body flex">
    <div>
      <canvas id="{{ id }}" class="mx-auto" height="80" width="80"></canvas>
    </div>
    <div class="text-right flex-grow">
      {% for content in contents %}
        <div class="flex alright-right">
          <div class="mr-1 flex-grow text-right" data-name="{{ sn }}:{{ content[prop] }}">{{ content[prop] }}</div>
          <div class="ml-1 flex-grow data-count text-right">{{ content['count']|number_format }}</div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>
